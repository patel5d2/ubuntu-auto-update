# Rust
/target
Cargo.lock

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test

# Node
node_modules
dist
.npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# General
.DS_Store
*.env
*.env.local

# Secrets & keys — never commit these
encryption.key
known_hosts
*.pem
*.key

# Temporary and test files
*.txt
*.tmp
*.log
test_*
*cookies*
*payload*

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Local Claude Code state / tooling (machine paths, dev creds — not for the repo)
.claude/settings.local.json
.claude/launch.json

# OS generated files
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Build artifacts and secrets that must never be committed
agent/target/
agent/auth.token
backend/migrate
backend/coverage.out
test-report.md
